/*
 * GithubRepos Messages
 *
 * This contains all the text for the GithubRepos container.
 */
import { defineMessages } from 'react-intl';

export const scope = 'app.containers.GithubRepos';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the GithubRepos container!',
  },
});
