/*
 * AsideSidebar Messages
 *
 * This contains all the text for the AsideSidebar component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.AsideSidebar";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the AsideSidebar component!"
  }
});
