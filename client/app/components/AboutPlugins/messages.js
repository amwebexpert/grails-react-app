/*
 * AboutPlugins Messages
 *
 * This contains all the text for the AboutPlugins component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.AboutPlugins";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "Backend plugins"
  }
});
