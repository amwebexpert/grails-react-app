/*
 * TopNavbarSearchForm Messages
 *
 * This contains all the text for the TopNavbarSearchForm component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.TopNavbarSearchForm";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the TopNavbarSearchForm component!"
  }
});
