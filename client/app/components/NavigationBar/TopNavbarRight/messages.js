/*
 * TopNavbarRight Messages
 *
 * This contains all the text for the TopNavbarRight component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.TopNavbarRight";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the TopNavbarRight component!"
  }
});
